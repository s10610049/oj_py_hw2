# OJ 现场演示指南

这份指南用于在本地演示题库、判题、AI 智能命题、多轮整改和管理员全员成绩面板。所有代码与 `scripts/seed_demo_activity.py` 保持一致，可直接复制；不包含账户密码、API Key 或本地绝对路径。

## 演示前准备

1. 按 [README](README.md) 启动 FastAPI 与 Streamlit。
2. 导入 24 道原创题：`uv run --locked python scripts/seed_demo.py --username your_username`。
3. 用普通账户登录网站，确认能打开 `DEMO-001`—`DEMO-006`。
4. 若要演示真实 AI，在后端本地 `.env` 中配置模型密钥；不要把密钥贴入页面截图或终端录屏。
5. 若要快速重建痕迹，运行 `scripts/seed_demo_activity.py` 或 `scripts/seed_demo_cohort.py`。两个脚本都会先备份 SQLite，且不会打印密码。

建议不要在演示前调用 `/api/reset/`；它会清空当前题目、账户、提交和 AI 会话。

## 推荐演示顺序

1. **登录与视觉**：展示分步登录、Logo、右上角语言/身份/AI 助手和侧栏动效。
2. **题库**：搜索 `DEMO`，演示洛谷难度颜色、知识点和每题个人状态。
3. **五类判题结果**：依次提交下方 AC、部分分、WA、CE、TLE 代码。
4. **成绩总览**：查看通过率、趋势、难度/知识点和结果分布是否随提交更新。
5. **AI 编程助手**：在题目页或提交详情展开助手，询问错误原因；确认上下文仅属于当前账号。
6. **智能命题**：选择知识点与目标难度，使用“基础命题 Prompt”生成首稿，展示具体百分比和停止按钮。
7. **生成中改要求**：点击“编辑要求”，加入“生成中改稿 Prompt”，再点击重新发送。
8. **首稿审查与整改**：首稿完成后输入“审查/整改 Prompt”，生成第二稿并对比修订历史。
9. **附件**：上传一份小型代码或题面参考文件，核对解析摘要后发起命题；演示结束后删除附件。
10. **题目导入**：下载标准题目包，展示安全预览与明确确认，不直接写入未经检查的 ZIP。
11. **管理工作区**：切换管理员，展示全员概览、账户列表、AC/部分分/WA/CE/TLE 分布与访问审计。
12. **中英文切换**：切换英文，确认导航、表单、成绩面板与已有英文译文的题面一起变化。

## 1. 服务、身份与课程 API

### 1.1 健康检查

点击路径：

1. 浏览器打开 `http://127.0.0.1:8000/api/health`。
2. 新标签打开 `http://127.0.0.1:8501/_stcore/health`。
3. 再打开 `http://127.0.0.1:8501`。

预期：后端返回 HTTP 200 且 JSON 中 `code=200`、`data.status=ok`；前端健康检查返回可用状态；网站显示品牌登录页。健康检查只证明进程响应，不代表登录、判题或 AI 已验收。

失败演示：停止后端但保留前端，再尝试登录。预期页面提示连接暂不可用，已经输入的用户名可保留，密码不会出现在错误文本中。演示后立即恢复后端。

### 1.2 注册、登录与账户页

点击路径：登录页 → “创建账户” → 输入一个未使用的演示用户名和自定密码 → 继续 → 确认创建；登录后侧栏 → “账户”。

预期：

- 注册成功后可以登录；密码输入不可见。
- 账户页展示用户名、角色、加入日期、用户编号、提交数和已通过题目数，不展示密码或哈希。
- 点击“退出登录”后回到登录页，原 Cookie 不再能访问受保护页面。

失败演示：

- 用同一用户名再次注册，预期 HTTP 400 的友好提示。
- 输入错误密码，预期 401，不应区分“用户不存在”和“密码错误”。
- 让管理员把一次性测试账户设为 `banned` 后再登录，预期 403。演示结束后可恢复为 `user`，不要禁用正在演示的主账户。

### 1.3 用交互文档核对课程 API

点击路径：`http://127.0.0.1:8000/docs` → 展开接口 → “Try it out”。先用 `POST /api/auth/login` 登录，再测试其他路由；用户名与密码使用自己的本地账户，不写入本文档。

建议依次核对：

| 操作 | 接口 | 成功现象 |
| --- | --- | --- |
| 题目列表 | `GET /api/problems/` | HTTP 200，`data` 是题目摘要列表 |
| 题目详情 | `GET /api/problems/DEMO-001` | 完整题面、样例、测试点与限制 |
| 语言列表 | `GET /api/languages/` | 至少有 `python`、`cpp` |
| 本人信息 | `GET /api/users/{自己的 user_id}` | 仅公开账户字段 |
| 本人提交 | `GET /api/submissions/?user_id={自己的 user_id}` | 分页摘要，不要求返回测试点明细 |

响应必须同时满足 HTTP 状态码和 `{code, msg, data}`。失败演示可使用下列低风险请求：

- 退出后请求 `GET /api/problems/`：401。
- 普通用户请求 `GET /api/users/`：403。
- 读取不存在的 `GET /api/problems/NOT-EXISTS`：404。
- 重复添加同一题号：409；已有题目不能被静默覆盖。
- 提交缺少 `code` 的 JSON：400，而不是 FastAPI 默认 422。
- 普通用户请求别人的提交详情或管理员重评接口：403。

提交频率限制演示只使用一次性账户：一分钟内连续发起超过课程限额的提交，最后一次应返回 429。不要为了展示 429 污染主 Demo 账户。

## 2. 学生题库、状态与提交闭环

### 2.1 搜索、筛选与分页

点击路径：侧栏 → “题库”。

1. 搜索框输入 `DEMO-00`，确认题号、中文标题或知识点都可命中。
2. 打开难度下拉，依次选择“入门”“普及-”“普及+/提高-”。
3. 清空筛选，使用题库页码切换页面。

预期：每道已知题只显示洛谷八级中的一个难度，不出现 `medium`、`easy` 或“旧制”；难度颜色不同。题目卡同时显示本人状态：未提交、评测中、已通过、部分通过、WA、CE、TLE 等。

失败演示：输入不存在的题号，预期空状态而不是页面报错；若个人状态接口短暂失败，题库仍可读，并提示状态暂不可用。

### 2.2 打开题面并提交

点击路径：题库 → 搜索 `DEMO-001` → “打开” → “代码与提交”标签。

1. 先查看“题目说明”标签中的题意、输入、输出、约束、样例和测试数据。
2. 在“代码与提交”选择 Python，粘贴本文后面的 AC 代码。
3. 点击“提交代码”。

预期：页面立即进入“提交记录”详情，最初显示评测中；前端自动轮询，完成后显示总分、编译/运行信息。返回题库时该题显示已通过。

失败演示：代码框留空点击提交，预期前端直接提示必填；输入会引发异常的代码，可得到 RE 而不是后台 500。

### 2.3 提交列表、详情与日志

点击路径：侧栏 → “提交记录”。普通用户的“提交用户”固定为本人；选择题号/状态 → “查询” → 选择提交编号 → “打开详情”。

预期：

- `pending` 期间每秒刷新，终态后停止自动轮询。
- CE 显示编译信息；WA/TLE/RE 显示总体运行诊断；代码放在可展开区。
- 点击“查询评测日志”后，私有日志只显示总分；题目允许公开测试点时才显示每点 AC/WA/TLE/MLE/RE、时间与内存。

权限失败演示：普通账户在“按编号直接查询”输入另一个用户的 submission id。预期详情请求 403；日志同样 403，并产生可由管理员审计的日志访问记录。

## 3. 双语与阅读体验

点击路径：登录后右上角语言按钮“中文/English”。建议在题库、题目详情、提交、成绩总览、智能命题、AI 助手和管理工作区各切换一次。

预期：

- 固定界面文案整页切换，不出现同一个难度下拉里一半中文一半英文。
- `DEMO-*` 题已有当前版本英文译文时，标题、题意、输入输出、约束、提示和标签随界面切换。
- 代码、题号、样例、测试点和数值限制保持原样。

失败演示：新建一题但不填写英文题面，切到 English。预期明确显示“Translation unavailable”或回退提示，不伪装成完整英文；随后在题目详情 → “管理英文题面”补齐并保存，再切换验证。修改中文原题后，旧英文稿应标记为过期，直到重新保存匹配版本。

## 4. 个人成绩总览

演示前先提交本文五类代码。点击路径：侧栏 → “成绩总览”。

预期展示：

- 尝试题数、通过题数、最佳得分、完成率等核心指标。
- 随日期变化的得分趋势。
- 洛谷难度、结果类型和知识点分布。
- 每题最佳提交、最新提交、当前版本关系和待处理状态。

验证方法：先记下指标，再回到题库对一题提交更好的代码，重新进入成绩总览。最佳得分与通过数应更新；后续一次 WA 不应抹掉历史已通过主状态。若刚提交的新任务仍在 `pending`，页面应单独提示，而不是把旧 AC 改成“未通过”。

失败演示：切换 English 后，未准备英文元数据的条目必须显示明确缺译提示，不能把中文标签混进英文图表。统计接口异常时页面显示数据不可用，不用零值伪装成功。

## 5. 手工出题、附件与导入

### 5.1 手工创建完整题目

点击路径：侧栏 → “题库” → “新增题目” → “手工出题”。使用一个尚未存在的题号，例如 `LIVE-ABS-001`：

| 字段 | 可复制内容 |
| --- | --- |
| 题目标题 | 绝对值距离 |
| 题目描述 | 给定两个整数，输出它们在数轴上的距离。 |
| 输入格式 | 一行两个整数 a 和 b。 |
| 输出格式 | 输出一个整数，表示 abs(a-b)。 |
| 数据范围与约束 | -10^9 <= a,b <= 10^9。 |
| 难度 | 入门 |
| 标签 | 整数运算, 绝对值 |
| 来源 | 现场原创演示 |
| 时间/内存 | 1 秒 / 128 MiB |

样例 JSON：

```json
[
  {"input": "3 10\n", "output": "7\n"},
  {"input": "-5 2\n", "output": "7\n"}
]
```

测试点 JSON：

```json
[
  {"input": "3 10\n", "output": "7\n"},
  {"input": "-5 2\n", "output": "7\n"},
  {"input": "0 0\n", "output": "0\n"},
  {"input": "-1000000000 1000000000\n", "output": "2000000000\n"}
]
```

参考提交：

```python
a, b = map(int, input().split())
print(abs(a - b))
```

预期：保存后跳到题目详情；再次创建同号题返回 409，不覆盖原题。编辑题目时题号锁定；管理员删除时必须先勾选确认。

失败演示：把样例 JSON 改成对象而不是数组，或去掉必填字段。预期表单保留已输入内容并指出格式错误，不产生半条题目。

### 5.2 手工表单内的 AI 整理

点击路径：题库 → 新增题目 → 手工出题 → 填入上面的半成品题面 → “AI 整理题目”。可填整理要求：

```text
保留“数轴距离”主题，补齐英文题面，检查极值边界，并生成不少于 6 个互不重复的测试点。不要改题号。
```

点击“AI 整理 / 补全”。预期显示百分比和阶段，可停止；成功草稿只回填当前表单，不自动保存。若生成期间手工改了表单，页面先提示冲突，必须点击“应用 AI 草稿到表单”才能覆盖。

失败演示：未配置模型时点击整理，应明确提示模型不可用且原表单/附件仍保留。

### 5.3 手工和智能命题附件

点击路径：手工出题表单下方“参考文件”，或侧栏 → 智能命题 → “参考文件（可选）”。

1. 上传一个不含隐私的 `.py` 或 `.md` 小文件。
2. 点击“解析所选附件”。
3. 核对文件名、类型、大小、内容预览、解析提示和到期时间。
4. 勾选“我已核对附件预览与解析提示”，再发起 AI 整理/命题。

预期：附件不会出现在保存后的公开题面；手工出题中也可选择“带入智能命题”，跳转后仍保留安全附件引用。

失败演示：选择超过单文件限制、宏 Office 文件、不支持扩展名或包含危险路径的 ZIP。预期在调用模型前拒绝；移除附件后可继续，不影响其他已确认附件。

### 5.4 标准 ZIP 与洛谷数据包导入

点击路径：题库 → 新增题目 → “从 ZIP 导入题目”。

标准包流程：

1. 点击“下载标准题目包模板”。
2. 不修改时直接选择该 ZIP，格式选“标准 OJ 题目包”。
3. 点击“上传并生成预览”。
4. 核对题号、标题、样例/测试点数量、语言、限制、摘要和过期时间。
5. 点击“确认导入题库”。

洛谷数据包流程：格式选“洛谷数据包”，手工补齐题号、题面、样例、难度、标签、来源及可选英文稿，再生成预览。只导入用户合法持有的数据；项目不会爬取或冒充洛谷官方隐藏测试点。

失败演示：

- 上传伪装成 ZIP 的文本：签名错误。
- ZIP 缺 `problem.json` 或数据文件：预览显示缺失字段，不可提交。
- 预览后修改文件或元数据：旧预览失效，必须重新生成。
- 题号冲突：显示当前摘要，必须勾选“确认覆盖现有题目”才可写入。
- 预览过期：确认按钮禁用，重新上传预览。

## 6. 日志策略与管理员重新评测

点击路径：管理员 → 题库 → 打开目标题 → “题目管理”。

1. 将 `public_cases` 关闭并保存。
2. 普通提交者打开自己的提交并查询日志：只能看到总分，不显示 `details`。
3. 将 `public_cases` 打开，普通登录用户再次查询：显示每个测试点状态、时间和内存。
4. 管理员进入提交详情 → “管理评测” → 勾选确认 → “重新评测”。

预期：重新评测覆盖原 submission id，先回到 `pending`，然后得到新终态；不会创建一个与原记录无关的新 id。

失败演示：普通用户调用重新评测或修改日志可见性，预期 403。切勿为了演示公开私有测试点的真实输入输出；日志公开只展示课程允许的状态、时间、内存等字段。

## 判题演示代码

### 1. AC：DEMO-001“两笔账目”

语言选择 **Python**：

```python
# OJ_DEMO_ACTIVITY:v1:accepted
a, b = map(int, input().split())
print(a + b)
```

预期：全部测试点通过，分数等于“通过测试点数 × 10”，题库状态显示已通过。

### 2. 部分分：DEMO-005“仓库装箱”

语言选择 **Python**：

```python
# OJ_DEMO_ACTIVITY:v1:partial
n, k = map(int, input().split())
print(n // k)
```

这段程序忘记向上取整，只在 `n` 能被 `k` 整除或 `n=0` 时正确。

预期：`0 < score < 测试点数 × 10`，未通过测试点为 WA，题库状态显示部分通过而不是被后续状态覆盖。

### 3. WA：DEMO-006“训练等级”

语言选择 **C++**：

```cpp
// OJ_DEMO_ACTIVITY:v1:wrong_answer
#include <iostream>
int main() { std::cout << "Z\n"; }
```

预期：编译成功，所有测试点答案错误，结果为 WA、得分为 0。

### 4. CE：DEMO-003“读书计划”

语言选择 **C++**：

```cpp
// OJ_DEMO_ACTIVITY:v1:compile_error
int main( { return 0; }
```

预期：编译失败、得分为 0，页面展示受限编译诊断，不出现运行信息。

### 5. TLE：DEMO-004“任务依赖”

语言选择 **Python**：

```python
# OJ_DEMO_ACTIVITY:v1:time_limit
while True:
    pass
```

预期：进程在题目时间限制后被终止，结果为 TLE、得分为 0。若操作系统策略阻断进程控制，应展示基础设施错误，不能把它伪装成 TLE 或通过。

## AI 智能命题 Prompt

点击路径：侧栏 → “智能命题”。页面从上到下依次是“模型与计价配置”、知识点/难度/要求/参考文件表单、当前任务、版本记录和首稿审阅区。

### 模型与计价配置

展开“模型与计价配置”：

1. 提供商 URL、模型名称使用本地环境或教师指定值。
2. API Key 只在需要新增或替换时输入；页面显示“后端已配置密钥”时留空会保留旧值。
3. 输入/输出单价可留空使用内置估算，也可填供应商当前单价；计价单位通常为 `1000000` Token，币种按填写值展示。
4. 点击“保存模型配置”。

预期：响应只显示密钥是否已配置，不回显密钥。更换提供商时必须重新输入密钥，避免把一个提供商的凭据误发给另一个地址。

失败演示：填写非法 URL、空模型名、负单价或空计价单位。预期调用前被拒绝；已有密钥不出现在错误信息。不要使用真实密钥做截图演示。

### 异步任务通用检查

任一 Prompt 发出后检查：

- 表单变灰并显示“本轮要求已锁定”，页面其他导航仍可用。
- 立即出现会话/任务编号、版本号、阶段文案、耗时、明确百分比和等长进度条。
- 点击“刷新进度”不会新建任务；离开页面再回来，终态仍可恢复。
- 点击“停止生成”后任务转为取消终态，后台不继续产生完成稿或新增费用。
- 展开“版本记录”，每次首稿、更新要求、整改都拥有独立任务、耗时、Token 和费用。

停止失败演示：在任务已完成后再次停止，预期 409/任务已经结束，不把成功稿改成取消。连接短暂中断时页面保留最后一次已确认状态，不声称后台已经停止。

### 基础命题 Prompt

知识点选择“整数与浮点数、条件分支、标准输入输出”，难度选择“入门”，命题要求粘贴：

```text
设计一道适合 Python 入门教学的整数奇偶判断题。题目必须给出清晰的标准输入输出、数据范围、至少两个样例、覆盖负数与零的测试点，以及可直接运行的 Python 参考解答。题目编号固定为 AI-DEMO-001，不依赖网络、随机数或第三方库。
```

自定义要求可填：

```text
语言专业、精简，边界条件可复核；输出仅使用 EVEN 或 ODD。
```

预期：立即返回任务/会话编号；页面在后台任务运行时保持可用，显示真实阶段、耗时和与填充长度一致的百分比；首稿通过结构与答案一致性检查后进入人工审阅区。2026-09-10 的真实浏览器验收已用同类“读入三个整数并输出极值差”需求跑通异步生成、数字进度和审阅入口；上面的 `AI-DEMO-001` 固定题号 Prompt 留作现场独立演示，重复演示时应换用未占用题号。

### 原始“多线程死锁检测”Prompt

下面是适合现场演示复杂边界和测试生成器的强化版要求。自动矩阵保留助教群中的原始死锁需求原文，并额外附加可独立复算的有向图 I/O 合同；两者不要混称为同一段 Prompt：

```text
为Python课程设计一道中等难度题：资源加锁顺序能否形成死锁风险。抽象为有向图判环，不运行真实线程。明确重复边、自环、不连通图。本次教学数据n<=100、m<=1000，预期O(n+m)。测试应区分正确线性解与每个起点重复枚举路径、不记忆已完成节点的自然低效正确解；至少构造一个多层汇合分叉、路径数量指数增长的无环图，不用早期环让慢解立即退出。保持合法边数和节点数，说明具体构造对应的复杂度，不声称已实测TLE。最终至少10个完整测试点（小型字面测例加生成器补充测例合计），含每种上述边界、起点所在分量无环而另一个分量有环、正式测试中的多组输入。n=100或m=1000的大测例只用test_generator循环构造，不要直接写进JSON字面测例。提示不得说自环不影响环的存在性。附可直接stdin/stdout运行的Python标准库reference_solution和答案核对说明。id使用AI-SMOKE-001。
```

预期审查要点：

- 将锁依赖抽象为有向图，不启动真实线程。
- 自环必须判定为环，重复边不应改变结论，不连通分量均需检查。
- 正确解复杂度为 `O(n+m)`。
- 大测试由受限生成器构造，JSON 不直接展开成百上千条边。
- 参考解、样例和所有生成测试的答案一致。

真实模型验收记录：`runtime/ai-matrix/ai-matrix-218dbc28517c49a19049f227f4c5af1e.json` 中原始死锁需求连续 3/3 次完成，耗时分别为 56.343、55.109 和 39.891 秒；三次均通过完整 authoring gate、独立有向环 oracle 和典型错解区分门。

### 生成中改稿 Prompt

在任务运行时点击“编辑要求”，把要求追加为：

```text
保留原题知识点与难度，但题目编号改为 AI-DEMO-EDIT-001；增加一个零输入样例，并明确负数奇偶性的数学定义。测试点必须包含 -1、0、1 和绝对值接近上界的奇数与偶数。
```

然后点击“重新发送”。预期：当前活动任务被实际停止或转为明确终态，新修订继承会话上下文并从更新后的要求重新生成；旧修订仍可查看。真实浏览器验收中的对应会话已记录“版本 1 cancelled → 版本 2 replace_requirements completed”，且页面实际观察到锁定、解锁、重新发送和数值进度。

准确点击路径：任务运行中 → 表单下方“编辑要求” → 修改知识点、难度、命题需求、自定义要求或附件 → “重新发送”。不想修改时点击“放弃编辑”，表单恢复当前版本原值。

并发失败演示：在另一个浏览器窗口先提交同一会话的新版本，再回到旧窗口重发。预期旧窗口得到版本冲突提示，不会覆盖新版本；刷新会话后才能继续。

### 首稿审查/整改 Prompt

首稿完成后在整改输入栏粘贴：

```text
在保持原题意和难度不变的前提下，补强零、负奇数和极大整数边界说明，并确认样例、测试点与参考解答完全一致；返回整改后的完整题目。
```

预期：产生新的 `refine_draft` 修订，不覆盖首稿；页面可对照修订编号、任务状态、Token 和估算费用。真实浏览器验收已继续生成“版本 3 refine_draft completed”，并确认版本 2 仍保留、版本 3 可进入审阅区。

准确点击路径：任务完成 → “审阅题目” → 选择任一成功稿 → 阅读/修改题号、题面、英文稿、样例 JSON、测试点 JSON、参考程序和生成说明 → 在“改进要求”粘贴内容 → “重新整改出题”。历史成功稿可以作为分支起点；选择旧稿时页面会明确提示将以该稿创建新版本，新修订完成后审阅区自动切换到最新成功稿。

失败演示：整改框留空时按钮不应发起模型请求；某次整改失败时，上一个成功稿仍可查看和保存，历史版本不会被覆盖。

### Token 与估算费用检查

任务进行及结束后核对“本轮用量”和“会话累计用量”：输入 Token、输出 Token、总 Token 与估算费用都应为有限非负数，累计值不小于任一单轮值。自动修正若发生，提供商调用次数和 Token 必须计入同一版本总量。

预期：提供商返回完整统计时优先使用真实数值；缺字段时只估算缺失部分，页面仍给出默认费用估算而不是“未知”。费用是依据配置单价的估算，不等同最终账单。

失败演示：把单价留空后生成，仍应显示有依据的默认估算；提供商完全不可用时任务失败，费用不得伪造为一次成功调用。最新 19 条真实模型矩阵的每条任务均保留提供商 Token 与有限非负的 CNY 估算，合计 323355 Token、约 5.216715 CNY；这只是本地配置口径的估算，不是供应商账单。

### 文件参考命题 Prompt

先上传一份不含个人信息的小型 `.py`、`.md`、`.txt`、`.json`、`.csv`、`.docx`、`.pdf`、图片或受限 ZIP，再输入：

```text
根据已解析的参考文件提炼一个可独立完成的 Python 编程题。只使用文件中明确出现的概念，不假设未提供的视觉或隐藏内容；题面必须脱离原文件也能读懂。给出标准输入输出、约束、至少两个样例、覆盖边界的自建测试点和可运行参考解。若文件信息不足，请明确指出缺口，不要编造。
```

预期：界面先展示文件名、类型、大小和解析摘要；模型收到的是有界解析快照，不是本地路径。超过单文件/总量/数量限制或不支持的格式应在调用模型前拒绝。当前真实浏览器验收已经完成手工出题附件的选择、解析预览、交给 AI 和移除控件，也实际走通标准 ZIP 的下载、安全预览、明确确认、英文题面和删除；越权与恶意文件失败路径由自动化回归覆盖。

## AI 编程助手现场问题

点击路径：任一登录后页面 → 右上角“AI 编程助手”。首次打开 → “新建会话”，助手应先发送简短能力介绍。抽屉支持历史会话、关闭、全屏、停止回答和删除会话；位置变化不应清空正在输入的内容。

建议分别在三处演示：

- 题目详情：助手知道当前题号和公开题面。
- 正在填写代码：助手可以分析用户显式选中的草稿，但不自动提交或执行。
- 提交详情：助手可以引用本人提交的公开诊断，不读取另一个账户的代码或私有测试点。

在 `DEMO-005` 部分分提交详情打开助手：

```text
请根据当前题目和这次提交的公开诊断，用三句话解释为什么只能得到部分分。先指出反例，再提示正确的整数向上取整公式；不要直接重写整份程序。
```

预期：回答提到 `n` 不能被 `k` 整除时 `n // k` 少算一箱，并给出 `(n + k - 1) // k`；不泄露私有测试点输入输出。

在 `DEMO-004` 题目页打开助手：

```text
只给我解题路线：比较 DFS 三色标记与 Kahn 拓扑排序，说明如何处理自环、重复边和不连通图，最后给出复杂度，不提供完整代码。
```

预期：语言专业、精简，遵从“不提供完整代码”，并仅引用当前用户显式选择的上下文。2026-09-10 的真实浏览器验收已确认助手介绍语、加载/停止状态、真实回复、移动端关闭和焦点恢复；指定题目上下文、取消恢复、权限隔离和失败保留均有 API/组件自动化反例覆盖。本节保留为现场展示脚本，便于观众直接观察这些行为。

停止/恢复演示：发送问题后在生成中点击“停止回答”，预期已收到文字保留且状态显示已停止；刷新历史后仍可看到。断开后端再发送，预期输入保留并提示连接失败；恢复服务后可直接重试。

隐私失败演示：在 A 账户记录一个独特代码片段，退出后登录 B 账户询问“A 账户刚才写了什么”。预期助手无法获取；即使手工输入 A 的 submission id，后端也按 B 的权限重新校验。

## 管理面板演示数据

多账户脚本的预期分布：

| 账户 | 预置判题类型 |
| --- | --- |
| `demo_aurora` | AC、部分分、CE |
| `demo_binary` | AC、部分分、CE、WA |
| `demo_cedar` | AC、部分分、CE、TLE |

每个账户还应存在一条完成的首稿和一条完成的整改稿；管理员全员概览应显示所有账户（包括零提交账户），并能比较尝试数、通过数、得分、通过率和结果分布。2026-09-10 06:31 的当前 API/SQLite 快照已核对 6 个账户、5 个启用账户、4 个有做题痕迹账户和 20 次提交；三名 `demo_*` 学习者各有 1 个命题会话、2 个完成修订，并分别呈现表中的 AC/部分分/CE 及 WA 或 TLE 差异。另保留一份浏览器验收生成的 AI 题目及 AC 提交，供现场展示“生成—入库—判题”闭环。

### 管理员完整点击路径

1. 登录管理员 → 侧栏“管理工作区” → “全员概览”。核对账户总数、活跃账户、提交、通过/得分、账户比较图、结果分布和明细表。
2. 切到“用户与权限”，选择一个一次性账户，选择 `user / admin / banned`，勾选确认后更新。
3. 展开“创建管理员账户”，只在确有需要时创建；密码不得出现在录屏或本文档。
4. 切到“日志访问审计”，按访问用户编号或题号筛选。
5. 切到“题目与评测”，通过按钮进入题库管理或提交查询/重新评测。

预期：零提交账户也出现在列表但指标为 0；多个账户的 AC、部分分、WA、CE、TLE 结果与上表一致。普通用户侧栏没有“管理工作区”，即使直接调用管理 API 也得到 403。

失败演示：更新角色但未勾选确认时不发送请求；选择已被其他管理员改变的账户状态后操作，应以服务端最新权限为准。不要在现场把唯一可用管理员禁用或降级。

## 最终证据登记区

以下内容由最终验收负责人在真实环境核对后填写，文档初稿不把“计划验证”写成“已经通过”：

| 证据 | 状态/位置 |
| --- | --- |
| 完整 pytest、Black、Flake8 | 同一冻结候选两次完整回归：A0 为 972 passed / 6 skipped / 138.91 秒，独立 A5 为 969 / 9 / 133.11 秒；均收集 978 项、0 failed。固定 4 项仅限 POSIX/Linux，另有 2/5 项被本机 Windows 执行策略间歇阻止；Black 97 文件、Flake8、diff check、锁文件和 66 包依赖检查全部通过 |
| Linux Python/C++、TLE/MLE/进程树 | 本机平台/策略跳过项由最终同 SHA 的 Ubuntu Python 3.10/3.12 CI 补证；Windows 结果不替代 Linux 证据 |
| 五类 Demo 提交及 submission id | 当前 `s10610049`：AC `9f94afbe24604be49bd63ebc7edb23d8`、部分分 `01f3523c83b84d3286d8e06841fe176c`、WA `93f6697455d54cd287bd7b89ffc07e71`、CE `31a2fff652bf44dfbeae9d1b12f178f3`、TLE `52745e16dc784ac2a457f3bdc9e7f313` |
| 三个 Demo 账户与管理员全员面板 | 06:31 快照：6 账户、5 启用、4 有做题痕迹、20 提交；三名 `demo_*` 账户画像及各 2 个完成命题修订均已核对，另保留 AI 题目到 AC 的完整闭环 |
| DeepSeek 首稿连续成功与复杂 Prompt 矩阵 | 本地忽略证据 `ai-matrix-218dbc…json`：19/19 completed；原始死锁 3/3，16 类矩阵全部有结果摘要并通过独立 oracle |
| 生成中编辑重发、停止、首稿整改 | 本地忽略证据 `browser-acceptance.json`：版本 1 cancelled、版本 2 replace completed、版本 3 refine completed，页面交互检查通过 |
| AI 助手上下文、停止与跨账户隔离 | 浏览器已验证介绍、加载/停止状态、真实回复、移动端 Esc 关闭与焦点恢复；用户边界、取消及失败恢复由 API/组件自动化回归覆盖 |
| 中文/英文与浏览器宽窄屏视觉 | 本地忽略证据 `browser-acceptance.json` 与 `browser-targeted-closure.json`：桌面与 390×844 共 96 项唯一交互通过、0 条浏览器错误、13 张报告截图齐全 |
| ZIP 导入、覆盖冲突与附件失败路径 | 浏览器已实际走通模板下载、安全预览、确认导入、英文题面、手工附件交接和删除；覆盖冲突、越权与恶意文件由自动化回归覆盖 |

## 演示验收清单

- [x] 所有页面均没有 Streamlit 的 Deploy 与右上角默认菜单。
- [x] 顶栏语言、身份和 AI 助手等高、单边框、右对齐，窄屏不重叠。
- [x] 中文/英文切换不出现同一控件中英混排；题面缺译文时明确提示。
- [x] 题库所有已知题目使用洛谷八级难度和对应颜色，不显示“旧制”。
- [x] 每题状态正确区分未提交、判题中、通过、部分通过、WA、CE、TLE 等。
- [x] AI 知识点为可搜索多选，目标难度为八级下拉，自定义要求可直接输入。
- [x] 生成进度同时显示具体百分比，条形长度与数字一致；停止操作真实生效。
- [x] 生成中可编辑要求并重新发送；首稿后可审查并多轮整改。
- [x] AI 命题和手工出题都能上传参考文件，错误文件在模型调用前失败。
- [x] 编程助手可从题目和本人提交上下文教学，不泄露其他账户或私有测试点。
- [x] 个人成绩面板与管理员全员概览随演示提交更新。
- [x] ZIP 题目先预览后确认；覆盖已有题目必须显式确认。
- [x] 普通用户、管理员和公开/私有日志权限符合预期。
- [x] 完整自动测试、格式检查、真实模型矩阵及浏览器验收分别留有证据。
- [ ] 发布后核对最终提交同 SHA 的 Linux 3.10/3.12 CI；本地 Windows 结果不冒充该外部证据。
