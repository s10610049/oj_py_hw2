# Copy this file to .env only if a local .env does not already exist.
# Never put a real API key in this example or in Git.
DEEPSEEK_API_KEY=
DEEPSEEK_BASE_URL=https://api.deepseek.com
DEEPSEEK_MODEL=deepseek-v4-flash
